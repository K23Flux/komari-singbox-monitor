# 安全策略

## 支持范围

安全更新优先覆盖最新发布版本。项目处于早期阶段，建议始终升级到最新 GitHub Release。

## 报告漏洞

请不要在公开 Issue 中披露可利用细节、Token、密码或服务器地址。请通过 GitHub 仓库的 Security 页面创建私密漏洞报告；如果该入口尚未启用，请先创建一个不含漏洞细节的 Issue，请求维护者提供私密联系方式。

报告建议包含：受影响版本、影响范围、复现条件、最小化复现步骤和建议修复方向。

## 项目安全边界

- Agent 不上传代理密码、UUID、Reality 私钥或完整 Sing-box 配置。
- 主控不会向 Agent 下发 Shell 命令，也不会修改或重启 Sing-box。
- 插件仅申请 Node 兼容模块和 HTTP 路由权限。
- nftables 规则仅计数，不执行放行、拦截、NAT、标记或限速。
